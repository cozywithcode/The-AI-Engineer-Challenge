# Optional: base URL for the chat API (FastAPI backend).
# - Local: run the backend on port 8000 and set this so the frontend can reach it.
# - Vercel: leave unset to use same-origin /api/chat (if API is deployed alongside the app).
# NEXT_PUBLIC_API_URL=http://localhost:8000
